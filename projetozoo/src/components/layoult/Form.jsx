import styles from './Form.module.css'

function Form({name, animalType, description, aliment, habitat}) {
    return (
        <div>
            <form>
                <label htmlFor=""></label>
            </form>
        </div>
    )
}

export default Form;